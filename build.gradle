plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.6'
    id 'io.spring.dependency-management' version '1.1.7'
}
ext {
    springAiVersion = "1.0.3"
}

group = 'com.kaust.ms'
version = '0.0.1-SNAPSHOT'
description = 'ms-manager-prompt'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

configurations {
    compileOnly {
        extendsFrom annotationProcessor
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // Library of webflux and testing
    implementation 'org.springframework.boot:spring-boot-starter-webflux'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    developmentOnly 'org.springframework.boot:spring-boot-devtools'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testImplementation 'io.projectreactor:reactor-test'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'

    // Library of security
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'com.google.firebase:firebase-admin:9.7.0'
    implementation 'org.thymeleaf.extras:thymeleaf-extras-springsecurity6'

    // Library aspects
    implementation 'org.springframework.boot:spring-boot-starter-aop'

    // Library of persist or database
    implementation 'org.springframework.boot:spring-boot-starter-data-mongodb-reactive'

    // Library of AI
    implementation 'org.springframework.ai:spring-ai-bom:1.1.0-M4'
    implementation 'org.springframework.ai:spring-ai-starter-model-openai'

    // Library of email
    implementation 'org.springframework.boot:spring-boot-starter-mail'
    implementation 'org.springframework.boot:spring-boot-starter-thymeleaf'

    // Library of report
    implementation 'net.sf.jasperreports:jasperreports-pdf:7.0.3'

    // Libraries documentation
    implementation 'org.springdoc:springdoc-openapi-starter-webflux-ui:2.8.13'

    // Library of logging cloud
    implementation 'com.google.cloud:google-cloud-logging-logback:0.132.18-alpha'

    // Library of mapping and constructor automatic
    compileOnly 'org.projectlombok:lombok'
    implementation 'org.mapstruct:mapstruct:1.6.3'
    implementation 'org.projectlombok:lombok-mapstruct-binding:0.2.0'
    annotationProcessor 'org.projectlombok:lombok'
    annotationProcessor 'org.mapstruct:mapstruct-processor:1.6.3'
}
dependencyManagement {
    imports {
        mavenBom "org.springframework.ai:spring-ai-bom:$springAiVersion"
    }
}

sourceSets {
    main {
        resources {
            srcDirs = ['src/main/resources']
            includes = ['**/*.properties', '**/*.yml', 'templates/**/*.html', '**/*.xml', "reports/**/*.ttf", "reports/**/*.xml", "reports/**/*.jasper", "reports/**/*.jrxml"]
        }
    }
}

tasks.named('test') {
    useJUnitPlatform()
}
