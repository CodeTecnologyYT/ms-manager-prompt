steps:
  # 1) Build image desde Dockerfile
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t", "europe-west1-docker.pkg.dev/dsb-development-446616/cloud-run-source-deploy/ms-manager-prompt/api-kaust:$COMMIT_SHA",
        "."
      ]

  # 2) Push de la imagen
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "push",
        "europe-west1-docker.pkg.dev/dsb-development-446616/cloud-run-source-deploy/ms-manager-prompt/api-kaust:$COMMIT_SHA"
      ]

  # 3) Deploy a Cloud Run
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run", "deploy", "SERVICE",
        "--image", "europe-west1-docker.pkg.dev/dsb-development-446616/cloud-run-source-deploy/ms-manager-prompt/api-kaust:$COMMIT_SHA",
        "--region", "europe-west1",
        "--platform", "managed",
        "--allow-unauthenticated"
      ]

images:
  - "europe-west1-docker.pkg.dev/dsb-development-446616/cloud-run-source-deploy/ms-manager-prompt/api-kaust:$COMMIT_SHA"

options:
  logging: CLOUD_LOGGING_ONLY